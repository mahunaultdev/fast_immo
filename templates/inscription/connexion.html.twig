{% extends 'base.html.twig' %}

{% block title %}Hello AccueilController!{% endblock %}

{% block body %}
<section class="formulaire">
  <div class="container margintop">
 

    <div class="jumbotron jumbotron-fluid col-md-7 mx-auto">
      <form class="mx-5">
        <div class="row mx-auto">


        </div>
        <div class="form-group col-md-12 required">
          <label class="texteb" for="email">E-mail</label>
          <input type="email" class="form-control" id="email" placeholder="Nom@gmail.com"
            pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[com,fr]{2,3}$">
        </div>

        <div class="form-group col-md-12 required">
          <label class="texteb" for="mdp">Mot de Passe</label>
          <input type="password" class="form-control" id="mdp" pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}"
            title="Doit contenir au moins une masjuscule, une minuscule et un chiffre et faire plus de 8 caractères">
        </div>

        <div class="row justify-content-center">
          <button type="submit" class="btn texteb">Connexion</button>
        </div>
      </form>
    </div>
  </div>
</section>

  
  




{% endblock %}